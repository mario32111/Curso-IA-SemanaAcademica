import numpy as np

#creaun arreglo de numpy
numeros = np.array([10,20,30,40,50])

print("suma:", np.sum(numeros))
print ("media:", np.mean(numeros))
print("raiz cuadrada: ", np.sqrt(numeros))
print("Arreglo original", numeros)
print("Arreglo ordenado:", np.sort(numeros))